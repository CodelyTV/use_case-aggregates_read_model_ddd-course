{
  "version": 3,
  "name": "ecommerce-gateway",
  "port": 8080,
  "endpoints": [
    {
      "endpoint": "/shop/products/{product_id}",
      "method": "GET",
      "backend": [
        {
          "host": ["http://host.docker.internal:3000"],
          "url_pattern": "/api/shop/products/{product_id}"
        }
      ]
    }
  ]
}
